# Environment Configuration Template
# =============================================================================
# IMPORTANT: This is a template file. DO NOT edit this file directly.
# Copy this file to create environment-specific configurations:
#   - .env.development: For local development and testing
#   - .env.production:  For production deployment
#
# Security: .env.development and .env.production are git-ignored
# =============================================================================

# Environment Type (development, production)
# Controls which config.yaml section is loaded and which database to use
# Default: development (fail-safe for testing)
ENVIRONMENT=development

# =============================================================================
# API KEYS
# =============================================================================

# OpenAI API Key (for embeddings and GPT-4o)
# Get from: https://platform.openai.com/api-keys
# Required for: Embedding generation (L2 insights), GPT-4o evaluations
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key (for Claude Haiku)
# Get from: https://console.anthropic.com/
# Required for: Haiku evaluations, reflexion framework
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# =============================================================================
# POSTGRESQL DATABASE CONFIGURATION
# =============================================================================
# Development: Uses cognitive_memory_dev database (test data, can be dropped)
# Production:  Uses cognitive_memory database (real data, backed up daily)
# =============================================================================

# PostgreSQL Connection Settings
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Database Name (environment-specific)
# Development: cognitive_memory_dev
# Production:  cognitive_memory
POSTGRES_DB=cognitive_memory

# PostgreSQL User and Password
POSTGRES_USER=mcp_user
POSTGRES_PASSWORD=your-postgres-password-here

# PostgreSQL Connection String (constructed from above)
# Development: postgresql://mcp_user:password@localhost:5432/cognitive_memory_dev
# Production:  postgresql://mcp_user:password@localhost:5432/cognitive_memory
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
# Development: DEBUG level for verbose logging
# Production:  INFO level to reduce log noise
# =============================================================================

# Log Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Overridden by config.yaml environment section if set there
LOG_LEVEL=INFO

# Log Format (text, json)
LOG_FORMAT=text

# =============================================================================
# SEMANTIC FIDELITY CONFIGURATION
# =============================================================================

# Fidelity Threshold for semantic density check (0.0-1.0)
# Default: 0.5 (requires >50% semantic content density)
FIDELITY_THRESHOLD=0.5

# =============================================================================
# MCP SERVER CONFIGURATION
# =============================================================================

# MCP Server Transport (stdio, http)
MCP_TRANSPORT=stdio

# MCP Server Host (for http transport only)
MCP_HOST=localhost

# MCP Server Port (for http transport only)
MCP_PORT=8000

# =============================================================================
# APPLICATION METADATA
# =============================================================================

# Application Version
APP_VERSION=3.1.0

# Application Name
APP_NAME=Cognitive Memory System

# =============================================================================
# DEVELOPMENT FLAGS (optional, development-only features)
# =============================================================================

# Enable Debug Mode (development only)
# Should be false or unset in production
DEBUG=false

# Enable Development Features (experimental features, mock APIs, etc.)
# Should be false or unset in production
DEV_FEATURES=false
